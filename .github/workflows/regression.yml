
name: regression

on:
  push:
  pull_request:

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run deterministic scorer smoke test
        run: echo '{"same_pickup":1}' | python tools/deterministic_scorer.py
