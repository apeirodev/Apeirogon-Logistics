# API Key Handling Responsibility Model

API keys must remain user-controlled. The project must not collect API keys. Local tools should store keys only in user-controlled configuration or environment variables.
