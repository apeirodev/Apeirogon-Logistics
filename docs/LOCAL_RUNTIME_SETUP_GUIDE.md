# Local Runtime Setup Guide

## Intended Future Runtime
- local app or CLI
- local storage
- optional provider adapters
- deterministic fallback mode
- local telemetry export

## First Implementation Target
A local-first tool that accepts manual mission entry and optional screenshots, then produces deterministic route recommendations.
