# Telemetry Privacy Guide

## Defaults
- telemetry contribution is opt-in
- raw screenshots remain local by default
- contributor identity is optional
- API keys are never collected by the project

## Sanitization
Remove unrelated overlays, private chat, account identifiers, and irrelevant personal information before public submission.
